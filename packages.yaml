version: 1
# WARNING: keys under the name of the package are mandatory: [requirements, requirements-make, requirements-check]
packages:
  pacman:
    requirements:
    requirements-make:
    requirements-check:

